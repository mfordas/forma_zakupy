import React from 'react';
import ShoppingListContent from '../../components/ShoppingList';


const ShoppingList = () => {
  
  return (
      <ShoppingListContent />
  );
};

export default ShoppingList;